# Root Source Slot

This repository is a pnpm/Turborepo monorepo. Canonical TypeScript source lives
under `packages/jinn` and `packages/web`; this root `src/` directory exists only
to satisfy Giles service-backend root-slot checks.
